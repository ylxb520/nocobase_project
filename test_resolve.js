const path = require.resolve('@nocobase/plugin-batch-download');
console.log('Resolved path:', path);
const p = require('@nocobase/plugin-batch-download');
console.log('Plugin loaded:', typeof p.default);
